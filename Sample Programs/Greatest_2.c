// Greatest of two nos, ifelse

#include <stdio.h>

void main() {
    int a = 45, b = 12;
    if(a > b)
        printf("A is greater");
    else
        printf("B is greater");
    getch();
}